---
phase: 03-end-to-end-chat-history-persistence-per-user
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, lib/services/chat.ts, app/api/chats/[id]/route.ts, app/api/chat/route.ts, app/(root)/agent/page.tsx, app/(root)/agent/[id]/page.tsx, context/agent-context.tsx, components/agent-client-page.tsx, types/chat.ts]
autonomous: true
requirements: [FLOW-07]

must_haves:
  truths:
    - "Each user account has its own isolated, persistent chat history"
    - "Each chat session supports a single PDF upload, and re-uploading replaces the previous one"
  artifacts:
    - path: "lib/services/chat.ts"
      provides: "Multi-user chat management services"
    - path: "components/agent-client-page.tsx"
      provides: "Sidebar showing active and historical chats with rename/delete capabilities"
---

<objective>
Implement isolated chat session persistence, multi-user isolation, and interactive session management.

Purpose: Replace guest_user with real authenticated users and implement full CRUD for chat history.
Output: Schema update, route handler modifications, UI state management updates, and history sidebar implementation.
</objective>

<execution_context>
@~/.gemini/antigravity/get-shit-done/workflows/execute-plan.md
@~/.gemini/antigravity/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-end-to-end-chat-history-persistence-per-user/03-CONTEXT.md
@prisma/schema.prisma
@lib/services/chat.ts
@app/api/chats/[id]/route.ts
@app/api/chat/route.ts
@context/agent-context.tsx
@components/agent-client-page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database Model and Chat Service CRUD Actions</name>
  <files>prisma/schema.prisma, lib/services/chat.ts</files>
  <read_first>prisma/schema.prisma, lib/services/chat.ts</read_first>
  <action>
    1. Add `title String?` to the `chats` model in `prisma/schema.prisma`.
    2. Run `npx prisma db push` to push the schema and regenerate the Prisma Client.
    3. Update `getUserChats(userId)` in `lib/services/chat.ts` to select and return `title` (falling back to first message content if title is null).
    4. Implement `renameChat(chatId, title)` and `deleteChat(chatId)` in `lib/services/chat.ts`.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - prisma/schema.prisma contains title under model chats
    - lib/services/chat.ts exports getUserChats returning title, renameChat, and deleteChat functions
  </acceptance_criteria>
  <done>Prisma client generated and DB helpers ready</done>
</task>

<task type="auto">
  <name>Task 2: API Endpoints and Authentication Logic</name>
  <files>app/api/chats/[id]/route.ts, app/api/chat/route.ts, app/(root)/agent/page.tsx, app/(root)/agent/[id]/page.tsx</files>
  <read_first>app/api/chats/[id]/route.ts, app/api/chat/route.ts, app/(root)/agent/page.tsx, app/(root)/agent/[id]/page.tsx</read_first>
  <action>
    1. Implement PATCH (rename) and DELETE (delete) handlers in `app/api/chats/[id]/route.ts`.
    2. Update `app/(root)/agent/page.tsx` and `app/(root)/agent/[id]/page.tsx` to read the `userId` cookie and redirect to `/login` if not authenticated.
    3. Retrieve initial user chats via `getUserChats(userId)` and pass them to `AgentClientPage`.
    4. In `app/api/chat/route.ts`, if a report already exists for the chat, update the `reports.file_path` to the new `fileUrl` and delete existing `lab_results` for the report instead of creating a duplicate report.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - app/api/chats/[id]/route.ts contains PATCH and DELETE methods
    - app/(root)/agent/page.tsx calls cookies() and redirects to /login if no userId cookie exists
  </acceptance_criteria>
  <done>Authentication and API endpoints implemented and secured</done>
</task>

<task type="auto">
  <name>Task 3: Client State Management and Sidebar UI</name>
  <files>context/agent-context.tsx, components/agent-client-page.tsx, types/chat.ts</files>
  <read_first>context/agent-context.tsx, components/agent-client-page.tsx, types/chat.ts</read_first>
  <action>
    1. Update `AgentContextType` in `types/chat.ts` to export `deleteChat` and `renameChat`.
    2. Implement `fetchHistory`, `deleteChat`, and `renameChat` API bindings inside `AgentProvider` in `context/agent-context.tsx`.
    3. Update the sidebar Sheet in `components/agent-client-page.tsx` to render the list of chats.
    4. Next to each chat item, display icons for Edit (Rename) and Delete. Implement inline renaming input and delete confirmation triggers.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - context/agent-context.tsx implements deleteChat and renameChat
    - components/agent-client-page.tsx displays history list and handles renaming and deletion triggers
  </acceptance_criteria>
  <done>Sidebar UI and client state fully synchronized</done>
</task>

</tasks>

<verification>
- [ ] `npx tsc --noEmit` succeeds without compilation errors
</verification>

<success_criteria>
- Multi-user isolation is enforced via cookies
- Users can create, load, rename, and delete chats from the UI
- Chat list dynamically loads and switching works without desyncs
- Uploading a PDF report to an existing chat replaces the old one and resets analysis state
</success_criteria>

<output>
After completion, create `.planning/phases/03-end-to-end-chat-history-persistence-per-user/03-01-SUMMARY.md`
</output>

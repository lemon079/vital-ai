---
phase: 03.1-complete-end-to-end-authentication-and-onboarding-flow
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/services/actions.ts, hooks/use-auth.ts, app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx, middleware.ts]
autonomous: true
requirements: []

must_haves:
  truths:
    - "Clean/wipe the database first so we start from a clean state"
    - "User cannot log in without a registered account"
    - "Redirect users to /onboarding immediately after successful login"
    - "Enforce onboarding completion and prevent revisit to login/signup after login"
---

<objective>
Configure and implement a fully functioning authentication and onboarding redirect flow.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Reset Database & Clear EP State</name>
  <files>prisma/schema.prisma</files>
  <read_first>prisma/schema.prisma</read_first>
  <action>
    Run npx prisma db push --force-reset to clean existing tables (users, chats, reports, lab_results, messages, profiles) and sync database schema.
  </action>
  <verify>npx prisma db push completes successfully</verify>
  <acceptance_criteria>
    - Neon database tables contain no legacy records
  </acceptance_criteria>
  <done>Database successfully wiped and reset</done>
</task>

<task type="auto">
  <name>Task 2: Synchronize Onboarding Status via Cookies</name>
  <files>lib/services/actions.ts</files>
  <read_first>lib/services/actions.ts</read_first>
  <action>
    - Update `signup` to set `isOnboarded` cookie to 'false'.
    - Update `login` to set `isOnboarded` cookie to 'true' (if is_onboarded) or 'false' (if not).
    - Update `submitOnboarding` to set `isOnboarded` cookie to 'true' upon successful profile saving.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - lib/services/actions.ts writes 'isOnboarded' cookies in signup, login, and submitOnboarding actions
  </acceptance_criteria>
  <done>Onboarding cookie synchronization implemented</done>
</task>

<task type="auto">
  <name>Task 3: Update Auth Hooks and Forms</name>
  <files>hooks/use-auth.ts, app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx</files>
  <read_first>hooks/use-auth.ts, app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx</read_first>
  <action>
    - Update `useSignin` in `hooks/use-auth.ts` to redirect to `/onboarding` on success.
    - Rewrite `app/(auth)/login/page.tsx` and `app/(auth)/signup/page.tsx` to use the real mutation hooks (`useSignin` and `useSignup`) and remove mock login simulation functions.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - hooks/use-auth.ts redirects useSignin to /onboarding
    - login/page.tsx calls useSignin hook
    - signup/page.tsx calls useSignup hook
  </acceptance_criteria>
  <done>Hooks and frontend forms connected to backend Server Actions</done>
</task>

<task type="auto">
  <name>Task 4: Implement Route Guard Middleware</name>
  <files>middleware.ts</files>
  <read_first>middleware.ts</read_first>
  <action>
    Create a global `middleware.ts` in the workspace root to check for `userId` and `isOnboarded` cookies:
    - If logged in, block access to auth pages (`/login`, `/signup`, `/register`) and redirect non-onboarded users trying to access `/agent` to `/onboarding`.
    - If not logged in, redirect any access to `/agent` or `/onboarding` to `/login`.
  </action>
  <verify>npx tsc --noEmit passes</verify>
  <acceptance_criteria>
    - middleware.ts exists in root directory
    - Matches all routes except static assets, _next and api
  </acceptance_criteria>
  <done>Global redirect middleware implemented</done>
</task>

</tasks>

<verification>
- [x] Run `npx tsc --noEmit` to verify type checking passes with zero errors
</verification>

<success_criteria>
- Clean database is verified
- User login checks email existence and hashed passwords
- Users are redirected to /onboarding upon login/signup
- Logged in users cannot revisit auth pages
- Non-onboarded users are forced onto /onboarding when trying to visit agent routes
</success_criteria>
